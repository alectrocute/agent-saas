{
  "name": "agents",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "db:migrate": "wrangler d1 migrations apply picohost --remote",
    "db:migrate:local": "wrangler d1 migrations apply picohost --local",
    "deploy": "nuxt build && wrangler pages deploy dist/"
  },
  "dependencies": {
    "@nuxt/ui": "^4.4.0",
    "nitro-cloudflare-dev": "^0.2.2",
    "nuxt": "^4.3.1",
    "nuxt-auth-utils": "^0.5.29",
    "tailwindcss": "^4.1.18",
    "vue": "^3.5.28",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "wrangler": "^4.66.0"
  }
}
